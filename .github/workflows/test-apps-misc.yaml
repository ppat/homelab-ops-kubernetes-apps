---
# yamllint disable rule:line-length
name: test-apps-misc

on:
  pull_request:
    paths:
    - '.github/workflows/test-apps-misc.yaml'
    - '.github/kind-cluster-single-node.yaml'
    - 'ci/test/apps-misc/**'
    - 'apps/subsystems/misc/**'
    - '!apps/subsystems/misc/**.md'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test-apps-misc:
    uses: ppat/github-workflows/.github/workflows/chainsaw-test.yaml@5a5a6e1b9ef08895f7a814de0eb0a8322a0bc3a0 # v3.3.1
    with:
      git_ref: ${{ github.head_ref || github.ref }}
      test_path: ./ci/test/apps-misc
      chainsaw_config: ./ci/test/chainsaw/.chainsaw.yaml
      kind_config: .github/kind-cluster-single-node.yaml
