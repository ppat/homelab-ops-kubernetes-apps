{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "packageRules": [
    {
      "automerge": false,
      "addLabels": [
        "automerge:off"
      ],
      "description": [
        "The bootstrap CRD copies never automerge. They exist so a first-time cluster build or a",
        "disaster recovery works: dependent modules reference custom resources before the module",
        "that normally installs those CRDs is deployed. A bad bump surfaces only on a path",
        "nobody exercises until they need it, so the gate is a human reading the diff, not a",
        "green suite."
      ],
      "matchFileNames": [
        "infrastructure/bootstrap/crds/**"
      ]
    },
    {
      "automerge": false,
      "addLabels": [
        "automerge:off"
      ],
      "description": "disable automerge for kubevirt -- minor releases have changed behaviour-affecting defaults (e.g. v1.6's VM rollout strategy default flip), every bump gets human review",
      "matchDatasources": [
        "github-releases"
      ],
      "matchPackageNames": [
        "kubevirt/kubevirt"
      ]
    }
  ]
}
