{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": [
    "A soak buys time for someone else to hit the bug first. All release-age policy lives here,",
    "and this preset is extended LAST in renovate.json: minimumReleaseAge is a plain scalar, so",
    "the last matching rule wins, and the two rules below that remove a soak must beat every",
    "earlier setter (the shared preset's per-update-type ladder, override-calver's flat 14 days).",
    "Keep this entry last in extends, and keep new release-age rules in this file rather than",
    "somewhere earlier where an unrelated preset can silently outrank them."
  ],
  "packageRules": [
    {
      "description": [
        "The inherited ladder gives a patch one day; a Longhorn regression is discovered when every",
        "PersistentVolume's data is on it, and a storage engine does not roll back the way it rolls",
        "forward. Seven days is chosen, not derived; the direction is the decision. Both spellings:",
        "the flux manager emits the chart name, the custom.regex annotations the GitHub slug."
      ],
      "matchPackageNames": [
        "longhorn",
        "longhorn/longhorn"
      ],
      "matchUpdateTypes": [
        "patch"
      ],
      "minimumReleaseAge": "7 days"
    },
    {
      "description": [
        "A Longhorn minor carries operator-facing upgrade steps (override-breaking-changes.json",
        "marks it breaking), so it gets a major's soak. Coder is the platform this repo's",
        "development happens on: a bad minor takes the tooling down, so it is soaked but not marked",
        "breaking; risk, not operator steps. 30 days, like the choice of these two dependencies, is",
        "chosen, not derived."
      ],
      "matchPackageNames": [
        "coder",
        "longhorn",
        "longhorn/longhorn"
      ],
      "matchUpdateTypes": [
        "minor"
      ],
      "minimumReleaseAge": "30 days"
    },
    {
      "description": [
        "Images this repo builds itself (docker.io/ppatlabs/*) are adopted as soon as published:",
        "the someone-else a soak waits for does not exist for a first-party build. Not a claim our",
        "images are safer.",
        "No matchUpdateTypes: an unlisted type would stay soaked by whatever set it last. null is",
        "the unset spelling; read it as zero (Renovate has no literal zero).",
        "Both spellings (registry-prefixed as the kubernetes manager emits, bare for annotations):",
        "a matcher matching nothing raises no error -- the previous glob was inert for two years",
        "(#3795); the idiom takes NO slash before the brace."
      ],
      "matchDatasources": [
        "docker"
      ],
      "matchPackageNames": [
        "ppatlabs{/,}**",
        "docker.io/ppatlabs{/,}**"
      ],
      "minimumReleaseAge": null
    },
    {
      "description": [
        "A digest refresh means the tag this repo runs was repointed at a rebuilt image (usually a",
        "CVE rebuild, the last thing to sit on); the version decision was taken when the tag was",
        "adopted. Soaking it is a trap, not a delay: Renovate ages the refresh against the tag's",
        "last repoint, so an image rebuilt more often than the soak never clears it, and under",
        "internalChecksFilter 'strict' the held branch is never created (no PR, no error); a tag",
        "with no release timestamp is held forever.",
        "Repo-wide because every docker dep is digest-pinned and an allowlist would not protect the",
        "next mutable tag added; docker-only because the github-actions SHA-pin soaks guard against",
        "tags moved to hostile code and must not be swept up.",
        "Digest updates only; version soaks and first-time pinning untouched. Ordering is",
        "load-bearing (file note above)."
      ],
      "matchDatasources": [
        "docker"
      ],
      "matchUpdateTypes": [
        "digest"
      ],
      "minimumReleaseAge": null
    }
  ]
}
