---
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: StepTemplate
metadata:
  name: validate-mcp-kubernetes-homelab
spec:
  try:
  - description: Assert service/mcp-kubernetes-homelab
    assert:
      resource:
        apiVersion: v1
        kind: Service
        metadata:
          name: mcp-kubernetes-homelab
          namespace: ai
  - description: Assert configmap/mcp-kubernetes-homelab-config
    assert:
      resource:
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: mcp-kubernetes-homelab-config
          namespace: ai
  - description: Assert serviceaccount/mcp-kubernetes-homelab
    assert:
      resource:
        apiVersion: v1
        kind: ServiceAccount
        metadata:
          name: mcp-kubernetes-homelab
          namespace: ai
  - description: Assert deployment/mcp-kubernetes-homelab
    assert:
      bindings:
      - name: name
        value: mcp-kubernetes-homelab
      - name: namespace
        value: ai
      file: ../chainsaw/assertions/deployment-ready.yaml
      timeout: 5m
  catch:
  - describe:
      apiVersion: apps/v1
      kind: Deployment
      name: mcp-kubernetes-homelab
      namespace: ai
  - events:
      namespace: ai
