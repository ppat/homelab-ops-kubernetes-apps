---
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: fake
spec:
  provider:
    fake:
      data:
      # cloudnativepg backup-credentials ExternalSecret mappings (components/db-backups) -
      # values match the fixed credentials ci/test/chainsaw/fixtures/object-store's
      # bootstrap Job imports, not a real secret.
      - key: "cluster_nas_minio_cloudnativepg_accesskeyid"
        value: "fixture-access-key"
      - key: "cluster_nas_minio_cloudnativepg_secretkey"
        value: "fixture-secret-key-not-a-real-secret"
