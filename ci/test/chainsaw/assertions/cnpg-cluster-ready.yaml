---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: ($name)
  namespace: ($namespace)
status:
  # Verify that the CNPG Cluster is ready
  (conditions[?type == 'Ready']):
  - status: 'True'
