---
# The credential minio's chart expects at `existingSecret`, supplied directly rather than
# through the ExternalSecret the storage-core module ships (which minio.yaml deletes).
#
# Why, and why it is not a loss of coverage: wherever this fixture is used, minio is
# *scaffolding* -- an S3 endpoint for something else -- never the subject. Resolving minio's
# own admin credential through external-secrets is storage-core's behaviour, and it is
# exercised properly by the infra-storage suite, where minio is the thing under test and keeps
# the ExternalSecret. A consuming suite's own ExternalSecrets are untouched and still resolve
# through its fake ClusterSecretStore.
#
# What it buys: minio's install was gated on external-secrets becoming ready, which measured
# T0+117s, so minio could not finish until T0+188s and everything waiting on minio inherited
# both. Taking the credential directly removes that link and lets minio install concurrently
# with external-secrets instead of behind it. It also means this fixture composes with nothing
# -- a suite needing only object storage need not stand up external-secrets at all.
#
# Measured on four infra-database runs, recorded because the first reading of them was wrong.
# The prerequisite reconcile came in at 57s, 136s, 135s, 116s -- it varies that much on its
# own, and an early two-point comparison mistook the 57s for a baseline and the 135s for a
# regression this change had caused. It had not. What this change actually moves is the minio
# wait: 2s and 2s with it, 2s and 100s without. Moving the wait late already takes minio off
# the critical path WHEN it happens to have converged by then; decoupling it from
# external-secrets is what makes that reliable instead of a coin flip. Totals were 399/403
# with against 341/465 without -- indistinguishable means at n=2, and a quarter of the spread.
# Narrowing the spread is the point.
apiVersion: v1
kind: Secret
metadata:
  name: minio-admin-credentials
  namespace: minio
type: Opaque
stringData:
  rootUser: minio-root
  rootPassword: root-passwrd-placeholder
