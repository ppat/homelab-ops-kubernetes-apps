---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- pod-blank.yaml
- pod-no-store-id.yaml

namespace: ${GUARD_NAMESPACE}

configMapGenerator:
- name: versitygw-sentinel-without-store-id
  files:
  - sentinel-without-store-id

generatorOptions:
  disableNameSuffixHash: true
