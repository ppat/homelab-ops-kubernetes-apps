---
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: StepTemplate
metadata:
  name: validate-harbor-exporter
spec:
  try:
  - description: Assert deployment/harbor-exporter
    assert:
      bindings:
      - name: name
        value: harbor-exporter
      - name: namespace
        value: harbor
      file: ../chainsaw/assertions/deployment-ready.yaml
  catch:
  - describe:
      apiVersion: apps/v1
      kind: Deployment
      name: harbor-exporter
      namespace: harbor
  - podLogs:
      selector: app.kubernetes.io/name=harbor,app.kubernetes.io/component=exporter
      namespace: harbor
  - events:
      namespace: harbor
