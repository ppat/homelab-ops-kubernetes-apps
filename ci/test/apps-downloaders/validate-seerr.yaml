---
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: StepTemplate
metadata:
  name: validate-seerr
spec:
  try:
  - description: Assert deployment/seerr
    assert:
      bindings:
      - name: name
        value: seerr
      - name: namespace
        value: downloaders
      file: ../chainsaw/assertions/deployment-ready.yaml
  catch:
  - describe:
      apiVersion: apps/v1
      kind: Deployment
      name: seerr
      namespace: downloaders
  - podLogs:
      selector: app.kubernetes.io/name=seerr
      namespace: downloaders
      tail: 1000
  - events:
      namespace: downloaders
