---
apiVersion: batch/v1
kind: Job
metadata:
  name: versitygw-store-prepare
  namespace: versitygw
spec:
  backoffLimit: 3
  ttlSecondsAfterFinished: 86400
  template:
    spec:
      restartPolicy: OnFailure
      automountServiceAccountToken: false
      containers:
      - name: prepare
        image: docker.io/alpine/k8s:1.37.0@sha256:b421c2e9419edb98db39b6ab641669f4db7bb2acf354f22450c6b7e7176d1ff4
        command: [sh, /scripts/prepare-store.sh]
        env:
        - name: VERSITYGW_STORE_ROOT
          value: /mnt/store
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: [ALL]
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1000
          runAsGroup: 1000
        resources:
          requests:
            cpu: 10m
            memory: 32Mi
          limits:
            memory: 64Mi
        volumeMounts:
        - name: data
          mountPath: /mnt/store
        - name: scripts
          mountPath: /scripts
          readOnly: true
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: versitygw-data-test
      - name: scripts
        configMap:
          name: versitygw-store-prepare-scripts
          defaultMode: 0555
      tolerations:
      - key: node-role.kubernetes.io/control-plane
        operator: Exists
