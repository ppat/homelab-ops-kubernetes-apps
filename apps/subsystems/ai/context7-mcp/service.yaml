---
apiVersion: v1
kind: Service
metadata:
  name: context7-mcp
  namespace: ai
  labels:
    app.kubernetes.io/name: context7-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: context7-mcp
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
