---
apiVersion: v1
kind: Service
metadata:
  name: mcp-kubernetes
  namespace: ai
  labels:
    app.kubernetes.io/name: mcp-kubernetes
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-kubernetes
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
