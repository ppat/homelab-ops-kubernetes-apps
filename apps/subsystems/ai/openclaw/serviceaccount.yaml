---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: openclaw
  namespace: openclaw
# Intentionally no RoleBinding: OpenClaw never talks to the k8s API directly -- cluster
# state/actions only go through the read-only Kubernetes MCP server (mcp-kubernetes),
# which has its own dedicated, scoped ServiceAccount.
