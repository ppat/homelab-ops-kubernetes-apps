---
apiVersion: v1
kind: Service
metadata:
  name: mcp-kubernetes-nas
  namespace: ai
  labels:
    app.kubernetes.io/name: mcp-kubernetes-nas
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-kubernetes-nas
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
