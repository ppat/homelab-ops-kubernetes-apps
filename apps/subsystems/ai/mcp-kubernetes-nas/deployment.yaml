---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-kubernetes-nas
  namespace: ai
  labels:
    app.kubernetes.io/name: mcp-kubernetes-nas
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mcp-kubernetes-nas
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mcp-kubernetes-nas
    spec:
      # No serviceAccountName: this instance talks to a remote cluster via the mounted
      # kubeconfig (below), so it must not inherit this pod's homelab identity.
      automountServiceAccountToken: false
      containers:
      - name: mcp-kubernetes-nas
        image: ghcr.io/containers/kubernetes-mcp-server:v0.0.65@sha256:5df586e2c7ced2a3125f6e78923388d80b69de0a2ad1470325b05318f12725bd
        imagePullPolicy: IfNotPresent
        args:
        - --port
        - "8080"
        - --config
        - /etc/kubernetes-mcp-server/config.toml
        env:
        - name: HOME
          value: /tmp
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        volumeMounts:
        - name: config
          mountPath: /etc/kubernetes-mcp-server
          readOnly: true
        - name: kubeconfig
          mountPath: /etc/kubernetes-mcp-server/kubeconfig
          readOnly: true
          subPath: kubeconfig
        - name: tmp
          mountPath: /tmp
        # The target (nas) cluster's API is reachable at the kubeconfig's endpoint, so
        # /healthz reflects real readiness rather than falling back to a TCP-only check.
        livenessProbe:
          httpGet:
            path: /healthz
            port: http
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: http
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            memory: 256Mi
        securityContext:
          runAsNonRoot: true
          runAsUser: 65532
          runAsGroup: 65532
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
      volumes:
      - name: config
        configMap:
          name: mcp-kubernetes-nas-config
      - name: kubeconfig
        secret:
          secretName: mcp-kubernetes-nas-secrets
      - name: tmp
        emptyDir: {}
