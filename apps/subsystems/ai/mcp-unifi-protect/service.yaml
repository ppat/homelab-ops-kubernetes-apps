---
apiVersion: v1
kind: Service
metadata:
  name: mcp-unifi-protect
  namespace: ai
  labels:
    app.kubernetes.io/name: mcp-unifi-protect
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-unifi-protect
  ports:
  - name: http
    port: 3001
    targetPort: http
    protocol: TCP
