---
apiVersion: v1
kind: Service
metadata:
  name: vault-loaded-exporter
  namespace: obsidian-vault
  labels:
    app.kubernetes.io/name: vault-loaded-exporter
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vault-loaded-exporter
  ports:
  - name: metrics
    port: 9877
    targetPort: metrics
    protocol: TCP
