---
apiVersion: v1
kind: Service
metadata:
  name: mcp-kubernetes-sandbox
  namespace: ai
  labels:
    app.kubernetes.io/name: mcp-kubernetes-sandbox
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-kubernetes-sandbox
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
