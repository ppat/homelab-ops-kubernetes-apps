---
apiVersion: v1
kind: Service
metadata:
  name: mcp-unifi-network
  namespace: ai
  labels:
    app.kubernetes.io/name: mcp-unifi-network
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-unifi-network
  ports:
  - name: http
    port: 3000
    targetPort: http
    protocol: TCP
