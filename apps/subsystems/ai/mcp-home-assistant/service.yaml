---
apiVersion: v1
kind: Service
metadata:
  name: mcp-home-assistant
  namespace: ai
  labels:
    app.kubernetes.io/name: mcp-home-assistant
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-home-assistant
  ports:
  - name: http
    port: 8086
    targetPort: http
    protocol: TCP
