---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- external-secrets/
# external-secrets-bitwarden/ is intentionally left out as its environments that need it can explicitly include it


sortOptions:
  order: fifo
